#ifndef MATH_H
#define MATH_H

/* angle in radians */
float sin(float angle);

/* angle in radians */
float cos(float angle);

/* angle in radians */
float tan(float angle);

float sqrt(float x);

#endif
